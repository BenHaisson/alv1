# Motion

Use Motion for React animations in this project.

```ts
import { motion } from "motion/react";
```

When a component needs Motion helpers, import them from the same package:

```ts
import { AnimatePresence, motion, useScroll, useSpring, useTransform } from "motion/react";
```

Keep animation behavior aligned with the existing components in `src/components`.
